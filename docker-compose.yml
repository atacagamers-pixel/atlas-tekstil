services:
  shop:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - ADMIN_USER=admin
      - ADMIN_PASS=admin123
      - TOKEN_SECRET=local-dev-secret-change-me
    restart: unless-stopped
